# 真实部署适配器 · 双轴审计记录（2026-08-21）

> 范围：`8230a14...HEAD`（身份/资产仓储 → 云台账投影 → SSH 执行 → 模型层 → 组合根，含修复提交 `8653264`/`90f1ee9`）
> 方法：code-review 双轴并行子代理（Standards = 质量基调 + smell 基线；Spec = 交接文档 §3 + ADAPTER-CONTRACTS + RQ）
> 结果：全量 361/361 绿；两轮审计（初审 → 修复 → 复审 → 再修复）

## 状态化清单（open/fixed/recorded）

### 第一轮初审发现 → 修复（`8653264`）

| # | 轴 | 发现 | 状态 |
|---|----|------|------|
| 1 | Standards | 原型链保留键拒绝缺失（exec params + asset id，第 12 波） | **fixed**（`8653264`，回归 R12/A6） |
| 2 | Standards | 模板/能力清单 JS/Python 双源 | **fixed**（shared-capabilities 单源 + Python WHITELIST 生成） |
| 3 | Spec | real 模式模型无法驱动执行（模型供应商无 interpretSync → execute 不可达） | **fixed**（handleAsync + sync 守卫显式报错，回归 F1） |
| 4 | Spec | exec 适配器未接入运行时链 | **fixed**（runJob execute→complete/fail，回归 F2/F3） |
| 5 | Spec | 身份仓储未接入矩阵判定（isAllowed 恒真） | **fixed**（matrixPort 角色投影，回归 F4） |
| 6 | Spec | async 兜底绕过 fail-closed | **fixed**（_finalize 统一白名单） |
| 7 | Standards | 死代码/数值校验/仓储重复/mock 归一化/参数名 | **fixed** |
| 8 | Spec | RQ-514 已退役挂起+通知 | **recorded**（M4 领域层返 REJECTED asset_retired 是领域语义；组合根不改领域判定，挂起+通知属领域层演进项） |
| 9 | Spec | clean_logs find 只读 | **recorded**（真实删除语义由远端脚本增强，注释+交接文档待办声明） |
| 10 | Spec | 范围蔓延（云台账投影/exec 失败语义超集/mock-rule） | **recorded**（台账投影=资产种子源可辩护；超集语义是三元组的细化非冲突；mock-rule 为测试基建） |

### 第二轮复审发现 → 修复（`90f1ee9`）

| # | 轴 | 发现 | 状态 |
|---|----|------|------|
| R1 | 双轴 | **keyVault 审计死代码假修复**（auditedKeyVaultPort 未接线 + F5 空转测试） | **fixed**（真接线 + F5 验证审计写入/脱敏断言） |
| R2 | Spec | handleAsync 单槽并发串包（A 消费 B 的意图 + 绕过 sync 守卫） | **fixed**（intent+actorId 归属队列，回归 F6） |
| R3 | Spec | matrixPort creator 错配（jobRepo 反查首个作业可借他人身份） | **fixed**（启动上下文绑定 target\|template → creator，单次消费） |
| R4 | Spec | runJob 参数映射缺口（scale/config/env 裸跑命令前缀） | **fixed**（全模板映射 + 缺参 failJob，回归 F7） |
| R5 | Standards | EXEC_CAPABILITIES 死导入 + 投机防御 | **fixed** |
| R6 | Standards | RESERVED_PROTO_KEYS 4 处定义成员不一致 | **fixed**（收敛 shared 单源，M3/M4 副本成员一致） |
| R7 | Standards | 三方能力/模板无同值锚定 | **fixed**（S1/S2 锚定测试） |
| R8 | Spec | intentId 纯文本幂等键跨 actor 误判 duplicate | **fixed**（actorId 前缀） |

## 残余（recorded，不阻塞）

- M3/M4 领域层常量仍为独立副本（shared 锚定测试防漂移；领域层引用 shared 属后续重构）
- mock 引擎未做小写归一（'Restart' 不命中——仅测试基建）
- clean_logs 真实删除语义（远端脚本增强，待办）
- RQ-514 挂起+通知（领域层演进项）
- **RAG 未定**：model-api `search()` 为声明式桩（对齐 §6 契约形状，恒返降级空结果，无调用方）——C5 知识检索未立项，接入时按 INV-K2 检索级 ACL 替换本桩

### 第三轮窄验证（`90f1ee9` 复审修正的独立确认）

| # | 验证点 | 结论 |
|---|--------|------|
| V1 | keyVault 审计真接线（F5 真验证） | ✅ 真修复 |
| V2 | handleAsync 并发归属队列 | ✅ 真修复（push→消费为同步原子区，findIndex 归属双保险；同 actor 同 intent 并发=可接受降级非 bug） |
| V3 | matrixPort 启动上下文绑定 | ✅ 真修复（M4 isAllowed 恰调一次，单次消费语义正确） |
| V4 | runJob 参数映射补全 | ✅ 真修复（五路齐全，缺参 failJob 可读） |

窄验证新发现 → 修复（`9d754f6`）：
- **N1**（低危）：M4 start 早退路径残留 `_pendingMatrixCtx` → **fixed**（finally 清除兜底，回归 F9）
- **N2**：F4 缺无上下文拒绝用例、F7 仅覆盖 scale 缺参 → **fixed**（F8 无上下文拒绝 / F10 缺参纵深——clean 缺 path 由 M4 构造拦截属领域防线，config_change 由 runJob 兜底）
- **N3**（接受）：F5 依赖真实 SSH 连接失败路径耗时 ~6s，脆弱性非正确性问题 → **recorded**

### 第四轮 E2E 审计（`26a2b1a` real 模式端到端冒烟）

| # | 轴 | 发现 | 状态 |
|---|----|------|------|
| C1 | Spec | **G2 断言失效**——测试直调 trust.resolveApproval 绕过编排层且硬编码 params，「意图/Grant/作业同 params」未被真实路径证明 | **fixed**（`c5ce125`：走 integration.resolveApproval 真实路径，params 取 r1.params 同源透传；顺带补上 INV-U5 审批审计+自动建作业） |
| A1 | Spec | 审计验证不完整——verify() 校验内存链非落盘内容 | **fixed**（从盘重读重建独立链 verify + 审批类留痕断言） |
| C2 | Spec | 失败放行过松——execution_failed 可能是远端真实缺陷 | **fixed**（放行收紧为网络三元组 + 失败落 failed 终态断言） |
| C3 | Spec | 「全量 366/366（1 skipped）」表述自相矛盾 | **fixed**（365 pass + 1 条件跳过如实表述） |
| S2/S5 | Standards | 注释宣称「台账单源」实为手抄字面量 | **fixed**（注释如实改为「手抄镜像」，声明投影有意剥离 ssh 防泄漏） |
| S3 | Standards | tmp 清理无 try/finally | **fixed** |
| S4 | Standards | G2 三处手抄字面量 | **fixed**（CLEAN_PARAMS 单源 const） |
| S1/B2 | 双轴 | 本地规则引擎与 compose mock 引擎语义重复；测试依赖仓库外个人私产（复现性） | **recorded**（E2E 属环境绑定冒烟非可移植用例；引擎抽取属后续重构） |
| B1/A2 | Spec | compose 免 Key 弱化 §3 原约束未回填 | **fixed**（交接文档 §3 已同步免 Key 行为） |
| C4 | Spec | 整链未走 handleAsync→Outbox 主通道 | **recorded**（Outbox deferred 路径已有 M5 integration 测试覆盖，E2E 直通路径为本轮范围）

## 最终状态

- 全量 365 pass + 1 条件跳过 = 366 tests（真实 SSH 链需 `VOYAGE_E2E_REAL=1`）
- 三轮审计链：初审（双轴并行）→ 修复 `8653264` → 复审（双轴并行）→ 修复 `90f1ee9` → 窄验证（独立子代理）→ 修复 `9d754f6`
- 窄验证结论：复审 4 项修复全部确认为真
