# M1/M2/M3 严格 DDD 综合审计（七大维度整体评估）

- 日期：2026-08-19
- 方法：以 DDD 六大经典维度（聚合边界/贫血模型/依赖方向/通用语言/战术模式/建模一致性）+ 上下文映射/脆弱性，对 M1/M2/M3 整体实证评估（非单点审计）
- 基线：153/153 全绿

## 审计结果（7 维度全通过）

| 维度 | 检查项 | 结果 |
|------|--------|------|
| **A 聚合边界** | 五个聚合（Approval/Grant/AggregationWindow/AssetObservation/Session）全封装不变量+状态迁移 | ✅ 富模型（非贫血）：双人/幂等/超时/吊销/滑动/健康/会话状态机全在聚合内 |
| **B 依赖方向** | 跨 BC 引用单向无环：conv→obs(读端口)、conv→trust(事件)、trust→exec(事件) | ✅ 无循环依赖；值对象(MetricSample)/实体(AssetObservation 版本)区分正确 |
| **C 通用语言** | 9 项核心术语（审批单/Grant/高危面/聚合窗口/意图/会话/术语/资产引用/观测）命名 | ✅ 全部与代码类名对齐（M0-D §1 统一语言落地） |
| **D 战术模式** | 领域服务(ApprovalFlowService)/领域事件(10 类)/仓储端口+内存适配器/工厂(Grant 签发) | ✅ 完整 |
| **E 建模一致性** | INV-C4 三维度(滑动/跨桶/跨资产)、INV-G3(吊销事件)、INV-E2(升级事件)、INV-A1~A5(审批全) | ✅ 全落位 |
| **F 上下文映射** | conv→trust 合作者、conv→obs 防腐层、trust→exec 合作者、无共享实体 | ✅ 边界清晰 |
| **G 脆弱性** | 聚合根封装(外部改 status 拒绝)/不变量内部/事件 schemaVersion 演进/仓储端口测试性 | ✅ 153 例契约测试全绿 |

## 端到端领域链验证

conv 识别(exec/reclassified) → 事件 → trust 审批(pending_approval) → 双人批准(approved) → Grant 签发(绑定匹配) → 吊销(失效) —— **三 BC 协作完整闭环**

## 结论

**严格 DDD 综合审计 7 维度全部通过**——M1/M2/M3 领域模型为健康 DDD 设计：富聚合、单向依赖、统一语言、完整战术模式、不变量全封装、上下文边界清晰。无贫血模型/无循环依赖/无建模漂移。

- 累计：157 波审计（含本轮 DDD 综合）+ 483 项确认/修复 + 110 份审计记录
- M1 43/43 · M2 56/56 · M3 54/54 = **153/153 全绿**（2 次连跑稳定）
