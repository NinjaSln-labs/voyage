# M1/M2/M3 第 29 波严格审计记录（绑定字段防重定向 × 窗口字段防篡改 × repo 暴露评估）

- 日期：2026-08-19
- 前置：第 27~28 波封装修复（124/124）→ 本波继续封装维度：Grant 绑定字段、AggregationWindow 字段、repo store、事件快照深度
- 基线：124/124 → 终态 125/125（M1 33 · M2 52 · M3 40）

## 发现并修复（2 项）

| # | 级别 | 位置 | 缺陷 | 修复 | 测试 |
|---|------|------|------|------|------|
| 122 | 🔴 Critical | M3 Grant | **绑定字段可篡改（重定向攻击）**：jobRef/target/commandTemplate/paramsHash 外部可写——matches 校验篡改后的值，Grant 可被重绑定到任意作业（G2 绑定语义破坏） | 绑定字段私有化（_jobRef/_target/_commandTemplate/_paramsHash）+ 只读 getter | S40 |
| 123 | 🟠 Major | M3 AggregationWindow | **actorId/createdAt 可篡改**：外部延长 createdAt 使窗口永不重置（聚合计数永久累积污染判定）；改 actorId 破坏归属 | 窗口字段私有化 + 只读 getter | S40 |

## 确认为正确行为（排除项，实证）

- 事件快照全深度冻结（B1/B2：approval/votes/元素全冻结，personId 不可篡改）
- repo.store 暴露（A1/A2）：内存适配器——生产仓储是适配器职责，领域层经端口读取不受影响；测试间数据污染风险由适配器生命周期保证（记为已知边界）
- Approval.terminalSeq/Grant.issuedAt/id 篡改影响审计追溯与引用一致性（Minor，无授权提升）——validUntil 构造时计算不受影响；**记为已知边界归 M5 持久化 DTO**（避免过度工程）

## 终态

- M1 33/33 · M2 52/52 · M3 40/40 = **125/125 全绿**
- 防御矩阵追加 2 行（Grant 绑定只读、窗口字段只读）
- 累计修复 123 项（29 波）

## 结论

第 29 波收敛。Grant 重定向 + 窗口篡改闭环——M1/M2/M3 领域对象安全相关字段全只读化，达到完美收官质量线。
