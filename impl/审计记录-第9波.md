# M1/M2/M3 第 9 波严格审计记录（语义面穷尽 × 白名单强制 × 组合验证）

- 日期：2026-08-19
- 前置：第 7+8 波修复（96/96）→ 本波对修复后代码做语义面穷尽 + 白名单强制 + 组合交互验证
- 基线：96/96 → 终态 102/102（M1 29 · M2 44 · M3 29）

## 发现并修复（7 项）

| # | 级别 | 位置 | 缺陷 | 修复 | 测试 |
|---|------|------|------|------|------|
| 52 | 🔴 Critical | M2 否定词面 | **否定词面缺口**：不想/不愿/不肯/拒绝/不重启（句首「不」+动词）与英文否定 don't/do not/never 全部判 exec——执行意图可被否定语义欺骗 | 否定词面补强：不想/不愿/不肯/拒绝 + (^|[^断停管])不+执行动词 + 英文否定 | S29 |
| 53 | 🔴 Critical | M3 白名单 | **白名单外能力自动 Grant**：rm_rf_root/shell_exec_any 等任意命令直接 auto_granted（INV-E3 附录 C「仅白名单可执行」未强制）——执行网关可签发任意命令许可 | WHITELIST_CAPABILITIES + QUERY_CAPABILITIES 白名单强制：非白名单 → REJECTED + CapabilityDenied 事件 | S27 |
| 54 | 🟠 Major | M2 疑问词 | **疑问词出现即 query 过度触发**：「那么/多么/什么/重启吗啡」误判 query（阻断真实执行「那么重启服务」） | 疑问词精确化：「么」不作独立疑问词；「吗」独立语气词判定（动词后+非啡/句尾）；明确句式（要不要/怎么/为什么/了吗/没有等） | S30 |
| 55 | 🟠 Major | M3 审批池 | **审批人池 <3 未校验**（INV-A4 硬约束缺失），空池静默通过 | 构造 fail-fast：resolvers() <3 → 抛错 | S28 |
| 56 | 🟡 Minor | M3 Grant | **TTL 无上限**（1 年通过，防永久授权缺失） | GRANT_MAX_TTL_MS 7 天，超限拒绝 | S29 |
| 57 | 🟡 Minor | M2 术语服务 | **undefined 返回 → 原生 TypeError**（`Cannot read properties of undefined` 泄露内部属性名，非领域错误） | 结构校验 fail-fast：须含 needsConfirm/needsTargetConfirm 布尔 | S31 |
| 58 | ✅ 确认 | M3 聚合窗口 | 非法能力拒绝后不计入聚合窗口（防攻击者刷窗口污染合法判定） | 实证确认（G3），不修 | — |

## 确认为正确行为（排除项，实证）

- 非否定含「不/别」不误伤：不断重启/不停重启/不管怎样重启 全 exec（S29）
- 真实祈使零误伤：帮我重启/重启服务吧/赶紧重启/重启服务啊 全 exec（S30）
- 白名单交互：query_status 3 次升级 → ApprovalRequested+AggregationEscalated；restart 高危每次审批（无冗余升级事件）；非法能力 10 次后合法 restart escalated=false（G3）
- prune 与容量协同：满窗 record 抛 AGG_WINDOW_LIMIT，旧事件出窗后 prune 释放可再记录（H）
- 补位授权发布 SubstitutionGranted；M2 事件序列：exec→Reclassified / query→Recognized / 否定→Recognized(query)（H2/H3）
- 跨会话 account 窗口（1h）闭区间边界语义正确（70 分钟恰在边界保留，80 分钟剔除）

## 终态

- M1 29/29 · M2 44/44 · M3 29/29 = **102/102 全绿**
- 防御矩阵追加 6 行（否定词面、白名单强制、疑问精确化、审批池、TTL 上限、术语结构）
- 累计修复 58 项（9 波）

## 结论

第 9 波收敛。M1/M2/M3 语义面穷尽 + 白名单强制 + 组合验证零缺陷——达到完美收官质量线，M4 执行闭环可在本基线上开工。
