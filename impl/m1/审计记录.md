# M1/M2 严格审计记录（代码级）

- 日期：2026-08-18
- 对象：impl/m1（obs 域）、impl/m2（conv 域）全部 .js
- 方法：不变量↔代码映射、注入/边界/竞态/失败路径实测、跨 BC 事件流、深拷贝/内存

## 发现并修复（7 项）

| # | 缺陷 | 严重度 | 修复 | 回归测试 |
|---|------|--------|------|---------|
| 1 | conv 事件类定义了但服务从不发布（conv→trust 事件流断裂，M2 声称的对接未实现） | Major | IntentRecognitionService 发布 IntentRecognized/Reclassified | S1/S2 |
| 2 | 事件载荷共享引用：发布后外部改 intent.type 污染事件（跨 BC 篡改） | Major | 事件构造时 freezeIntent 冻结不可变快照 | S4 |
| 3 | 「恢复」执行动词误伤纯查询（服务恢复了吗→执行类） | Major | 移除「恢复」；疑问句（吗/了吗/了没）一律查询 | S3/S6 |
| 4 | 置信度 NaN/Infinity/越界未拒绝（模型返回异常值穿透） | Major | Intent 构造加 Number.isFinite 校验 | S7 |
| 5 | obs 日志数组无限增长（被管机日志洪泛=内存 DoS） | Major | recordLog 容量上限（默认 10000）+ LOG_CAPACITY_EXCEEDED | S1(obs) |
| 6 | 超长输入（5000 字）不拒绝（输入防护只声明未落地） | Major | recognize 长度 1~4096 校验 | S5 |
| 7 | downIf 策略异常向上抛崩溃 / snapshotFor(trusted) 无日志明细 | Minor | 策略异常→unknown 不编造；trusted 见日志明细、public 仅计数 | S2/S3(obs) |

## 复验证据

- M1：14/14 通过（含新增 S1/S2/S3）
- M2：20/20 通过（含新增 S1~S7）
- 深拷贝核验：snapshot at→ISO 字符串、篡改隔离
- 并发写：版本递增无竞争
- 不变量↔代码映射：11 项全部落地
- 危险调用扫描：0（evaluateHealth 为误报）

## 结论

**严格审计通过（收敛）**：7 项缺陷全部修复并回归。M1/M2 达到契约基线，可接 M3 trust 域。
