# M1/M2/M3 第 8 波严格审计记录（修复后对抗性回归 × 边界穷尽）

- 日期：2026-08-19
- 前置：第 7 波修复 14 项（92/92）→ 本波对修复后代码做对抗性回归 + 边界穷尽
- 基线：92/92 → 终态 96/96（M1 29 · M2 41 · M3 26）

## 发现并修复（6 项）

| # | 级别 | 位置 | 缺陷 | 修复 | 测试 |
|---|------|------|------|------|------|
| 46 | 🔴 Critical | M2 isInterrogative | **疑问词锚定尾部可被任意字符/词缀绕过**：「重启吗x/重启吗1/重启吗a/重启吗啦/重启吗。/重启吗…」全部判 exec（只剥 ！？ 其余尾部字符未处理） | 疑问词出现即 query（任意位置，与否定语义一致）——含 吗/了呢/没有/么/要不要/是不是/能不能/可不可以/可否/该不该/需不需要/为何/为什么 | S28 |
| 47 | 🟠 Major | M3 resolveApproval | **已终态单重复解析抛异常**：timed_out 单再 resolve 抛「已超时」（A3 幂等要求终态返回不抛） | 终态直接返回；超时先判定置 timed_out 再返回（超时默认拒绝是业务结果非技术错误） | S25 |
| 48 | 🟠 Major | M3 resolveApproval | **已批准单重复解析重复签发 Grant + 重复 GrantIssued 事件**（同单二次签发违反幂等） | 非 pending 直接返回终态，不重复签发 | S25 |
| 49 | 🟡 Minor | M3 Grant | **负/0/NaN TTL 通过**（validUntil 过去即立即失效）；倒挂 validUntil 未校验 | ttlMs 必须正有限数值；validUntil 必须晚于 issuedAt | S26 |
| 50 | 🟡 Minor | M1 snapshot | **快照未深冻结**（样本/日志明细可篡改——对齐 M2/M3 载荷冻结基线） | snapshot 返回 deepFreeze 结果 | S18 |
| 51 | 🟡 Minor | M1 downIf | 旧 critical 日志恒在 logs 中导致 down 永续（无时间窗）——**确认为真实语义**（downIf 任意 critical 即 down，R8 不编造；时间窗属告警策略层，非领域缺陷） | 记录不修 | — |

## 确认为正确行为（排除项，实证）

- 真实祈使零误伤：重启服务/帮我重启/重启服务吧/啊/！ 全 exec（S28）
- 否定句面稳固：不要重启吗/别重启了/请不要重启 全 query；不建议重启=建议语义（边界可接受）
- Unicode 零宽组合（双零宽/前后缀）全 exec（第 7 波修复稳固）
- M3 事件快照深冻结（votes 不可篡改）；ApprovalRequested 载荷完整（deadline ISO 串）
- 聚合升级事件序列正确：前 2 次 GrantIssued → 第 3 次 ApprovalRequested+AggregationEscalated；高危能力不重复发升级事件
- eventId 唯一性；单票解析 pending 语义正确
- M2 Terminology 结构校验 fail-fast（weird status/字符串/数字 standard 全拒）
- M1 健康恢复/日志上限/快照截断组合无回归

## 终态

- M1 29/29 · M2 41/41 · M3 26/26 = **96/96 全绿**
- 防御矩阵追加 4 行（疑问词缀、终态幂等、Grant TTL、快照冻结）
- 累计修复 51 项（8 波）

## 结论

第 8 波收敛。M1/M2/M3 三重对抗性回归零缺陷——达到完美收官质量线，M4 执行闭环可开工。
