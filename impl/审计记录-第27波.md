# M1/M2/M3 第 27 波严格审计记录（封装性 × 不可变语义 × 读接口副作用）

- 日期：2026-08-19
- 前置：第 7~26 波（119/119，审计收敛）→ 用户要求继续 → 换全新维度：封装性与不可变语义（内部状态引用暴露、读操作副作用、返回对象可变性）
- 基线：119/119 → 终态 123/123（M1 33 · M2 52 · M3 38）

## 发现并修复（5 项，含 2 Critical）

| # | 级别 | 位置 | 缺陷 | 修复 | 测试 |
|---|------|------|------|------|------|
| 115 | 🔴 Critical | M3 Approval | **votes 数组引用暴露**：外部 `ap.votes.push()` 伪造第二票 → resolve() 判 approved（**绕过 WebAuthn 双人审批达成批准**） | votes getter 返回冻结拷贝；内部 _votes；status 只读 getter（内部 _status） | S38 |
| 116 | 🔴 Critical | M3 Grant | **revokedAt/validUntil 字段可写**：外部 `g.revokedAt=null` **撤销吊销**、`g.validUntil=2030` **延长 TTL**（安全吊销/有效期可绕过） | revokedAt/revokedReason/validUntil 只读 getter（内部 _revokedAt/_validUntil） | S38 |
| 117 | 🟠 Major | M1 AssetObservation | **metrics Map / logs 数组引用暴露**：外部 `obs.metrics.set()` / `obs.logs.push()` 注入伪造观测（破坏 R8 真实观测）；latestMetric 返回内部样本引用（latest.value=999 污染健康判定） | metrics/logs getter 返回拷贝；latestMetric 返回深冻结快照 | S21 |
| 118 | 🟡 Minor | M1 snapshotFor | **denied 快照未冻结**（可篡改 securityLabel） | denied 路径 deepFreeze | S22 |
| 119 | 🟡 Minor | M2 Session | **summary/turns/rotated 字段可外部替换/篡改**（summary 替换为伪造摘要、turns 置大） | summary/turns/rotated 只读 getter（内部 _summary/_turns/_rotated） | S39 |

## 确认为正确行为（排除项，实证）

- turns/rotated 篡改影响有限（置大=自伤 DoS、置真=锁死自己，无授权提升）——但为一致性仍封装修复
- 修复后读接口全部隔离：外部 set/push/替换全被拒，内部状态不受污染

## 终态

- M1 33/33 · M2 52/52 · M3 38/38 = **123/123 全绿**
- 防御矩阵追加 3 行（votes 伪造、吊销撤销、读接口引用隔离）
- 累计修复 119 项（27 波）

## 结论

第 27 波收敛。封装性维度暴露 2 个 Critical（审批伪造、吊销撤销——均属**安全不变量破坏**，第 7~26 波未覆盖此维度）。修复后 M1/M2/M3 领域对象全只读化——达到完美收官质量线。
