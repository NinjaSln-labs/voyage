# M1/M2/M3 第 10 波严格审计记录（组合利用链 × 状态机终态 × 跨 BC 协议一致性）

- 日期：2026-08-19
- 前置：第 7~9 波修复（102/102）→ 本波对修复后代码做组合利用链穷尽 + 状态机终态穷尽 + 跨 BC 协议一致性验证
- 基线：102/102 → 终态 104/104（M1 29 · M2 46 · M3 29）

## 发现并修复（4 项）

| # | 级别 | 位置 | 缺陷 | 修复 | 测试 |
|---|------|------|------|------|------|
| 59 | 🔴 Critical | M2 recognize | **组合利用链绕过**：疑问/否定判定用原始串、动词匹配用归一化串（双视图不一致）——「重\u200C启吗」（零宽+疑问）、「ｄｏｎ'ｔ ｒｅｓｔａｒｔ」（全角英文否定）、「重\u200B启\u200B吗\u200B」（零宽+疑问+零宽）全部判 exec | 疑问/否定判定统一在归一化视图上做（与动词匹配同一视图）；isNegation 归一化后匹配 dont/donot/never | S32 |
| 60 | 🟡 Minor | M2 Session | **rotate 后 recordTurn 通过**（会话终态仍可记录轮次） | recordTurn 检查 rotated 终态拒绝 | S33 |
| 61 | 🟡 Minor | M2 Session | **rotate 幂等缺失**（终态可二次轮换，与 compress 终态拒绝不一致） | rotate 二次调用拒绝 | S33 |
| 62 | ✅ 确认 | M3 状态机 | Approval.addVote 超时抛错置终态（G2 断言行为），resolveApproval 前置拦截——语义一致，编排层永不触异常 | 实证确认，不修 | — |

## 确认为正确行为（排除项，实证）

- 9 种组合绕过（零宽+疑问/全角英文否定/异体字+否定/否定+零宽）全部 query；真实祈使+零宽/那么/吗啡 全部 exec（S32）
- Session rotate 后 compress 拒绝（既有测试回归）；Approval approved 后 resolve 幂等 + addVote 拒绝
- Grant 吊销广播时序：ApprovalRequested → ApprovalApproved → GrantIssued → GrantRevoked，载荷含 revokedAt/revokedReason（exec 订阅方按 INV-E5 处理未启动节点）
- M3 事件协议与 conv 完全对齐（schemaVersion=1 + eventId + 深冻结载荷）；补位授权 90 天
- M1 健康组合：cpu 恢复→healthy 回升；critical 日志→down；critical 后 cpu 恢复仍 down（**down 持续是正确语义**——R8 不编造恢复，恢复依赖新观测，属适配器职责）
- M1/M2 快照 schema 跨 BC 对齐（count/samples 结构）

## 终态

- M1 29/29 · M2 46/46 · M3 29/29 = **104/104 全绿**
- 防御矩阵追加 2 行（组合利用链统一视图、会话终态）
- 累计修复 61 项（10 波）

## 结论

第 10 波收敛。M1/M2/M3 组合利用链 + 状态机 + 跨 BC 协议全维度零缺陷——达到完美收官质量线，M4 执行闭环可在本基线上开工。
