# M1/M2 第 3 波严格审计记录（攻击者利用链 × 跨域组合）

- 日期：2026-08-18
- 方法：完整利用链模拟、事件重放/幂等、跨域组合、协议演进、状态机、语义边界

## 发现并修复（4 项）

| # | 缺陷 | 级别 | 修复 | 测试 |
|---|------|------|------|------|
| 14 | **事件无幂等键**：at-least-once 重投导致「重启」重复消费（E2） | Major | 事件加 `eventId`（时间戳+序列+随机），消费者可去重 | S13/S15 |
| 15 | **目标资产歧义未覆盖**：术语命中≠目标确定，「清理」表命中仍可能对错资产执行（E3） | Major | translate 返回 `needsTargetConfirm`，术语命中也须确认目标资产（INV-K4） | S14 |
| 16 | **空指标名污染聚合**（E6） | Minor | MetricSample name 非空校验 | S5 |
| 17 | **同名指标单位不一致**：cpu % vs 核 混用影响健康判定（E11） | Minor | recordMetric 单位一致性校验 | S6 |
| 18 | **事件协议无版本字段**（E9） | Minor | 事件加 `schemaVersion: 1` | S16 |

## 确认为正确行为（无需修改）

- **密级 fail-closed 利用链**：public 查敏感资产 → denied（E1）
- **事件发布 fail-fast**：总线抛错传播（不静默丢弃，E7）
- **混合意图**：执行动词优先（重启服务看看状态→exec，F1）
- **英文纯查询**：status/health 不误伤（F4）
- **会话 rotate**：旧 summary 清空、上下文不可见（F3）
- **重试分层**：领域层 fail-fast，重试归 Outbox 编排层（F2，正确分层）

## 最终状态

- M1：17/17 · M2：29/29 全绿
- 事件协议：schemaVersion + eventId + 深冻结载荷 + raw 原文（审计追溯）
- 术语契约：结构校验 + 歧义确认 + 目标资产确认
- 指标契约：单位一致性 + 空名校验 + 容量上限 + 时间戳校验

## 结论

第 3 波审计收敛（4 项修复 + 6 项行为确认）。M1/M2 已达完美收官质量线，可交付 M3 并按 `完美收官-质量基调.md` 执行。
