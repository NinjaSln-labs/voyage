# 审计记录 — 评测门禁执行机制 + CRL 吊销镜像

## 元信息

- **日期**：2026-08-25 · **审计方**：接收 session（ox-alpha）+ 独立子代理（安全对抗视角）
- **对象**：eval-gate.js（三集制配套：快照绑定/隐藏集隔离/回滚钩子）+ crl-mirror.js（吊销集自动化）+ 评测集子目录布局迁移
- **流程**：先审后提交（独立子代理盲审 → 修复锚定 → 全量回归）

## 发现清单

| # | 级别 | 轴 | 发现 | 处置 | 锚定 |
|---|------|----|------|------|------|
| F1 | **P0** | 安全 | CRL 源返回空数组 → 清空全部吊销集 = 全量解除吊销（已吊销证书重新可信任）；「成功但空」是源故障典型形状，与 fail-closed 冲突 | ✅ fixed：默认拒绝 `empty_source` 保留原集；确需清空显式 `allowEmpty:true` | crl-mirror.test.js C7 |
| F2 | P2 | 正确性 | samples.json 顶层为数组/标量时静默降级为空集，绕过规模与哈希校验 | ✅ fixed：顶层形状校验，非法显式抛错 | eval-gate.test.js G7 |
| F3 | P2 | 一致性 | 绕过领域 ModelGated——手拼平面对象缺 eventId、未冻结，下游消费者语义断裂 | ✅ fixed：发布真实 ModelGated 实例（eventId+深冻结）；判定仍单次 evaluate（gate() 重复计算的性能取舍已注释） | G5 |
| F4 | P2 | 性能 | 差量计算 removed 用数组 includes O(n²) | ✅ fixed：Set 判定 O(n) | （等价重构，C1 行为回归覆盖） |
| F5 | P2 | 一致性 | 公开集 manifest 维护者为占位角色，易被误读为满足独立岗要求 | 📝 recorded：模块头部声明口径——独立岗要求针对隐藏/红队集（领域 ≥2 强制），公开集占位不替代 | eval-gate.js 头注 |
| F6 | P3 | 正确性 | 同 setType 多子目录合并语义策略文档未定义 | 📝 recorded：当前确定性合并（ids 拼接），待真实部署出现场景再裁决 | — |

## 四轴结论

- **安全轴**：P0 空源防御修复后 fail-closed 闭环；快照只存哈希+得分无样本内容泄漏面；审计条目无指纹值（C4 锚定）
- **正确性轴**：防重入语义正确（单线程 event loop）；畸形输入全部显式抛错（G7）
- **一致性轴**：ModelGated 领域实例化后与 M6 事件协议对齐；评测集布局单源迁移（runner/测试同路径映射更新，无双源）
- **测试充分性轴**：负路径补齐（G7/C7/C8）；stop 后手动刷新正交性锚定

## 回归证据

```
新增契约测试：eval-gate 7 例 + crl-mirror 8 例
全量基线：401 → 416 tests（415 pass + 1 条件跳过，0 fail）
```
