# M1/M2/M3 第 28 波严格审计记录（封装修复补全 × 剩余暴露面）

- 日期：2026-08-19
- 前置：第 27 波封装修复（123/123）→ 本波验证修复完整性 + 剩余暴露面（浅冻结残留、events 暴露）
- 基线：123/123 → 终态 124/124（M1 33 · M2 52 · M3 39）

## 发现并修复（2 项）

| # | 级别 | 位置 | 缺陷 | 修复 | 测试 |
|---|------|------|------|------|------|
| 120 | 🔴 Critical | M3 Approval.votes | **第 27 波修复不完整**：votes getter 只 `Object.freeze([...])` 浅冻结——数组冻结但**元素 ApprovalVote 的 personId 仍可写**：`ap.votes[0].personId='evil'` 后 resolve 判 approved（双人审批仍可伪造） | votes 返回深冻结元素数组（map+deepFreeze）+ 数组本身冻结 | S39 |
| 121 | 🟠 Major | M3 AggregationWindow.events | **events 数组引用暴露**：外部 `win.events.push()` 可伪造窗口计数（污染聚合升级判定/DoS） | events 私有化（_events）+ 只读 getter 返回深冻结拷贝 | S39 |

## 确认为正确行为（排除项，实证）

- A1 events 拷贝隔离生效：外部 push 后 countSameKind 仍 0（push 的是拷贝）
- A3 票元素深冻结生效：personId 不可篡改，双人审批真实达成（approved + 票保持 sre-1）
- S23 容量测试改为真实 record 填满（getter 无 setter 后原直接赋值测试修正）

## 终态

- M1 33/33 · M2 52/52 · M3 39/39 = **124/124 全绿**
- 防御矩阵追加 2 行（votes 深冻结、events 隔离）
- 累计修复 121 项（28 波）

## 结论

第 28 波收敛。封装修复补全（深冻结/私有化全覆盖）——M1/M2/M3 领域对象全只读化，达到完美收官质量线。
