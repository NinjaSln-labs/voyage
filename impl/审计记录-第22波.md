# M1/M2/M3 第 22 波严格审计记录（覆盖盲区 × 跨 BC 互操作 × 事件构造防护）

- 日期：2026-08-19
- 前置：第 7~21 波（116/116）→ 本波换维度：未测路径抽查、跨 BC 契约互操作、事件构造畸形输入防护、冻结有效性（strict 模式实证）
- 基线：116/116 → 终态 119/119（M1 31 · M2 51 · M3 37）

## 发现并修复（3 项）

| # | 级别 | 位置 | 缺陷 | 修复 | 测试 |
|---|------|------|------|------|------|
| 102 | 🟡 Minor | M3 AggregationWindow | **windowType 无枚举校验**（'weird' 通过——M0-D §5 定义 session/account/cross_bucket/asset） | 枚举校验拒绝 | S36 |
| 103 | 🟡 Minor | M1/M2/M3 事件构造 | **null 输入抛原生 TypeError**（`Cannot read properties of null (reading 'assetId'/'type'/'id')` 泄露内部属性名） | 事件构造 null fail-fast 明确领域错误（sample/entry/intent/approval/grant 必填） | S37/S38 |

## 确认为正确行为（排除项，实证）

- 事件载荷冻结**在 strict 模式生效**（TypeError），非 strict 静默失败——S4 断言正确（前次「篡改成功」是脚本非 strict 模式误判）
- 跨 BC 互操作：M1 快照 → M2 obs-query 消费（found/health/cpu 贯通）；M2 事件 → 载荷冻结 + JSON 往返保留语义
- 覆盖盲区抽查：无票直接 reject → rejected；webAuthn=false 拒绝；source=hack 拒绝；capability=undefined → 白名单 REJECTED（全正确）
- AccessEvidence 缺字段 → layered（缺层正确语义）

## 终态

- M1 31/31 · M2 51/51 · M3 37/37 = **119/119 全绿**
- 防御矩阵追加 2 行（windowType 枚举、事件构造 null 防护）
- 累计修复 103 项（22 波）

## 结论

第 22 波收敛。覆盖盲区 + 互操作 + 构造防护全维度——M1/M2/M3 达到完美收官质量线。
