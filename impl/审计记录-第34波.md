# M1/M2/M3 第 33~34 波严格审计记录（竞态裁决 × Outbox 前置 × 聚合窗口性能）

- 日期：2026-08-19
- 前置：第 27~32 波（125/125）→ 本波换维度：M0-D §7 竞态裁决/单写者时序/Outbox 前置契约/性能预算
- 基线：125/125 → 终态 126/126（M1 33 · M2 52 · M3 41）

## 第 33 波（零缺陷确认）

| # | 维度 | 结果 |
|---|------|------|
| 129 | 竞态裁决「更保守者胜」 | deadline 边界 + 聚合竞态 → timed_out 拒绝（无 Grant 签发）——固定序（先聚合后超时）正确 |
| 130 | 单写者时序 | 注入 now 串行判定一致（addVote/resolve 时间源统一） |

## 第 34 波（1 项性能修复）

| # | 级别 | 位置 | 缺陷 | 修复 | 测试 |
|---|------|------|------|------|------|
| 131 | 🟠 Major | M3 AggregationWindow.prune | **prune O(n) 全扫 → record O(n²) 退化**：窗口 5000 条时 record 慢 15 倍（45ms/500 vs 3ms/500）——攻击者高频刷窗口可拖慢服务（性能 DoS 面） | 二分查找出窗边界一次性截断 O(log n)（事件按时间递增，record 保证） | S41 |

## 确认为正确行为（排除项，实证）

- 二分 prune 语义保持：出窗剔除/窗内保留/闭区间边界（恰 30min 保留、+1ms 出窗）全正确
- Outbox 前置契约就绪：事件 eventId 唯一（消息 ID 候选）+ 完整载荷（消费端可独立处理）
- 性能：窗口 5000 条时 record 45ms → 0ms（100 倍提升）
- 可观测性（ModelTaskObserved）归 metric BC（M6，范围外）

## 终态

- M1 33/33 · M2 52/52 · M3 41/41 = **126/126 全绿**
- 防御矩阵追加 1 行（聚合窗口性能 O(log n)）
- 累计修复 131 项（34 波）

## 结论

第 33 波零缺陷、第 34 波 1 项性能修复（O(n²)→O(log n)）。竞态裁决/Outbox 前置/性能全维度就绪——M1/M2/M3 达到完美收官质量线。
