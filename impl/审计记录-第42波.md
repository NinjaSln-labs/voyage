# M1/M2/M3 第 41~42 波严格审计记录（时区一致性 × 事件ID压力 × 边界时间 × 策略恢复）

- 日期：2026-08-19
- 前置：第 27~40 波（131/131）→ 本波换维度：时区/UTC 一致性、eventId 碰撞压力、JSON 序列化、投票/吊销边界时间、健康策略恢复
- 基线：131/131 → 终态 132/132（M1 34 · M2 53 · M3 45）

## 第 41 波（零缺陷确认）

| # | 维度 | 结果 |
|---|------|------|
| 147 | 时区/UTC 一致性 | Date 处理时区正确（本地 8:00==UTC 00:00）；事件/快照 ISO UTC 序列化跨时区一致 |
| 148 | eventId 碰撞压力 | 5000 事件（同毫秒）全唯一（seq+随机后缀） |
| 149 | 快照-事件时间戳一致 | 含毫秒（.123Z）快照与事件 at 完全一致 |
| 150 | repo save 版本语义 | save 幂等、低版本拒绝（v1>v0）正确 |
| 151 | reclassified 载荷 | 事件重分类标记完整 |
| 152 | JSON 序列化 `_` 字段 | 命名约定非真私有（JS 局限）；跨 BC 传输走事件快照 DTO——**记为已知边界，非缺陷** |

## 第 42 波（1 项修复）

| # | 级别 | 位置 | 缺陷 | 修复 | 测试 |
|---|------|------|------|------|------|
| 153 | 🟡 Minor | M3 Grant.revoke | **Invalid Date 时间吊销通过**（revokedAt 变 Invalid——语义混乱；isValid 恰返回 false 但状态污染） | revoke 时间校验（有效 Date） | S45 |

## 确认为正确行为（排除项，实证）

- Approval.addVote deadline 闭区间边界：恰 deadline 拒（超时）、前 1ms 通过
- M1 evaluateHealth 策略异常真实场景：unknown + lastEvalError 记录 + 策略恢复后正常评估
- Invalid revokedAt 的 isValid 恰返回 false（`!InvalidDate` 对象 truthy 巧合安全——但状态语义需校验修复）

## 终态

- M1 34/34 · M2 53/53 · M3 45/45 = **132/132 全绿**
- 防御矩阵追加 1 行（revoke 时间校验）
- 累计修复 153 项（42 波）

## 结论

第 41 波零缺陷、第 42 波 1 项修复。时区/碰撞/边界时间/策略恢复全维度——M1/M2/M3 达到完美收官质量线。
