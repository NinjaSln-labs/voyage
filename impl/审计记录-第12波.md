# M1/M2/M3 第 12 波严格审计记录（原型污染 × 并发重入 × 事件流闭环 × 序列化安全）

- 日期：2026-08-19
- 前置：第 7~11 波修复（111/111）→ 本波换维度：原型键污染、并发/重入、事件流闭环、序列化安全、资源耗尽面
- 基线：111/111 → 终态 113/113（M1 31 · M2 49 · M3 33）

## 发现并修复（2 项）

| # | 级别 | 位置 | 缺陷 | 修复 | 测试 |
|---|------|------|------|------|------|
| 73 | 🟠 Major | M1 MetricSample | **原型链保留键作指标名**：`__proto__`/`constructor`/`toString` 通过——快照 `snap.metrics.toString` 从函数变对象（原型语义污染），消费方按名读取得到污染数据 | name 拒绝原型链保留键（__proto__/constructor/prototype/toString/hasOwnProperty/valueOf） | S20 |
| 74 | 🟡 Minor | M2 TermEntry | **oral 原型链保留键通过**（如 repo 用对象字面量查找则污染） | oral 拒绝原型链保留键 | S36 |

## 确认为正确行为（排除项，实证）

- M3 能力白名单天然拒绝原型键（`__proto__`/`constructor` 不在白名单 → REJECTED）
- 聚合窗口 capability 原型键仅用于 countSameKind 过滤（不构造对象键，无污染风险）
- 并发投票安全：双人交错 → approved，重复投票被 A3 幂等拒绝
- 事件流闭环完整：ApprovalRequested → ApprovalApproved → GrantIssued → GrantRevoked
- 全部事件（M1/M2/M3）JSON 可序列化（跨 BC 传输契约）
- Grant 到期 isValid=false（validUntil 语义正确）
- M1 事件发布归适配器层（仓储 eventLog，契约正确）；快照与事件字段一致；M2 返回与事件载荷一致
- 4096 字输入识别 2ms（无性能问题）

## 终态

- M1 31/31 · M2 49/49 · M3 33/33 = **113/113 全绿**
- 防御矩阵追加 2 行（原型链保留键）
- 累计修复 74 项（12 波）

## 结论

第 12 波收敛。原型污染 + 并发 + 事件流 + 序列化全维度零缺陷——M1/M2/M3 达到完美收官质量线。
