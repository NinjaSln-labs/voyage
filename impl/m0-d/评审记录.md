# M0-D 详细设计评审记录（严格审计版）

- 评审日期：2026-08-18（首轮评审后追加严格评审审计）
- 评审对象：`impl/m0-d/DDD设计.md`
- 评审方式：**对抗式严格审计**——不信任自评，独立取证：全量 RQ 提取比对、跨聚合事务机制核查、四层准入/数据对象/统一语言/接口失败语义逐项核查。

## 结论：✅ 通过（PASS，经严格审计修复后）

## 一、严格审计发现的缺陷与修复（首轮自评遗漏项）

| # | 缺陷 | 严重度 | 修复 |
|---|------|--------|------|
| 1 | **RQ→不变量显式追溯不足**：58 RQ 中仅 27 个被设计显式引用，31 个缺显式映射（虽 BC 隐含覆盖，但审计标准要求逐条可追溯） | Major | 新增 §2.12 RQ→不变量**全量追溯矩阵 58 行**（RQ- 规范化），58/58 全覆盖、0 未映射 |
| 2 | **观测域（C7）无任何不变量**（RQ-311~315 全缺） | Major | 新增 INV-O1（真实观测/数据非指令/内存 <50MB/告警阈值/只读口径） |
| 3 | **四层准入（R6）只有一句声明无机制** | Major | 新增 INV-T1（mTLS/WebAuthn 注册双通道+恢复/行为指纹/操作审批四层，缺层拒绝或分层动作） |
| 4 | **跨 BC「同事务」机制未定义**（RQ-623 契约固化要求未落地） | Major | §7 补事务边界机制：本地事务+Outbox（审批-执行-审计链）、降级态落盘缓冲、Saga 补偿（批量）、不可用即拒绝 |
| 5 | **矩阵服务端强制无 INV 编号** | Minor | 新增 INV-P1（矩阵唯一口径+特权动作覆盖） |
| 6 | **术语表无受管聚合定义** | Minor | 新增 INV-K4（双人审阅+全量回归+歧义确认+变更触发门禁） |
| 7 | **通知幂等（RQ-822）未入不变量** | Minor | 并入 INV-N1 |
| 8 | **月度评估（RQ-715）未显式映射** | Minor | 新增 INV-M5（与漂移监控联动） |
| 9 | **接口失败语义未定义**（只有成功语义） | Minor | §4 补三态返回（OK/REJECTED/ERROR）+ fail-closed/降级/超时语义 |

## 二、修复后复验证据

- **RQ 追溯**：`comm` 比对 58/58 全覆盖，**0 未映射**（修复前 31 个缺口）。
- **不变量**：40 条（原 35 + 新增 O1/T1/P1/K4/M5 + N1 扩展）。
- **R 规则**：R1–R11 全部映射（R6→T1、R8→O1、R10→K4 等）。
- **事务机制**：Outbox/Saga/落盘缓冲三类边界定义落位。
- **接口失败语义**：三态返回 + fail-closed 落位。
- **红蓝 10 项 Critical/Major**：仍全部固化（无回归）。

## 三、遗留（非阻断，属 M1 实现验证）

- Outbox 幂等消费、哈希链演练、NTP 部署、设备指纹防克隆强度——实现期验证。
- 聚合阈值/告警阈值/校准值——M1 实测基线校准（指标口径双态原则）。

## 四、结论

**严格评审审计通过**：自评遗漏的 4 项 Major + 5 项 Minor 已全部修复并复验（58/58 RQ、40 不变量、事务机制落位）。设计可作为 M1+ 实现与测试的契约基线。

## 五、严格审计循环（第 1~5 轮，直至无问题）

| 轮 | 审计角度 | 发现 | 修复 |
|----|---------|------|------|
| 1 | 追溯矩阵单元格精确性 / 执行中吊销 / 接口-时序 / 数据对象 | F1 矩阵 3 单元格错配（RQ-212/223/513）；F2 执行中作业吊销语义未定义（红蓝 R-07 残留）；F3 缺 `trust.issueGrant`；F4 数据对象缺 5 实体 | 矩阵修正；INV-G3/E5 吊销中止语义；issueGrant 接口；补会话/审计事件/术语条目/高危面清单/通知实体 |
| 2 | 事件闭包 / 同步调用环 / 时序-接口映射 | F5 数据对象缺 FAQ 条目/聚合窗口状态 | 补 FAQ 条目（审阅状态机）+ AggregationWindow 实体 |
| 3 | 事件订阅者 / 实体字段-不变量 / 跨账户聚合 | F6 AggregationWindow 缺资产级窗口（跨账户）；补位授权无记录实体 | 窗口支持资产级；补 SubstitutionGrant 实体 |
| 4 | 幂等 / 并发 / Outbox / 时间依赖 | F7 Outbox 消费端幂等/重试/死信未定义 | §7 补消息 ID 幂等去重+退避重试+死信告警 |
| 5 | 聚合边界 / 术语 / 补位回收 / 评测绑定 | **无新缺陷** | — |

**循环收敛**：第 5 轮无任何缺陷 → 严格审计循环结束。最终状态：**42 不变量、58/58 RQ 全覆盖、13 数据对象实体、16 事件订阅者闭环、接口-时序全映射、Outbox/吊销/幂等语义完备**。设计达到严格审计基线。

## 六、严格审计循环（第 6~12 轮，追加，直至连续零缺陷）

| 轮 | 审计角度 | 发现 | 修复 |
|----|---------|------|------|
| 6 | 状态机事件/设备实体化/统一语言/输入限流 | F8 FAQ 状态机缺迁移事件；F9 设备级准入无实体；F10 统一语言零定义；F11 对话层无输入限流 | 补 FaqStatusChanged 事件；Device 实体；统一语言核心术语定义 10 条；输入防护（4096 字符/30 次每分钟） |
| 7 | 边界竞态/分布式时钟 | F12 审批超时×聚合升级竞态无裁决；F13 多节点时钟一致性未定义 | §7 补「更保守者胜」串行裁决 + 单写者时序 |
| 8 | 事件-不变量闭包/判定点序/实体完备 | **无** | — |
| 9 | 补位-矩阵原子性/性能预算/可观测性 | F14 审计先行性能预算缺失；F15 可观测性指标未映射 | §7 补性能预算（审计快路径+哈希链异步）+ ModelTaskObserved 事件 |
| 10 | 事件完整性/数字一致性 | F16 Grant 有效期「24h」vs「24 小时」措辞不一致 | 统一为「24 小时」 |
| 11 | 状态机/白名单/术语/聚合边界 | **无** | — |
| 12 | Saga 补偿/错误分类/保序/幂等/度量对接 | **无** | — |

**追加循环收敛**：第 11、12 轮连续两轮零缺陷 → 严格审计循环终结。最终状态：**42 不变量、58/58 RQ、17 事件、16 数据对象实体、统一语言 10 定义、输入防护/竞态裁决/时钟一致性/性能预算/可观测性全落位**。设计达严格审计基线，可交付 M1+ 实现。
