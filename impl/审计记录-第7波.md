# M1/M2/M3 第 7 波严格审计记录（对抗性复核 × 不变量实证）

- 日期：2026-08-19
- 方法：对 M1/M2/M3 全部源码做对抗性复核——Unicode 语义欺骗、边界竞态、状态粘滞、事件协议、聚合语义、生产健壮性；**每项发现均先实证复现再修复**（非推测）
- 基线：M1 25/25 · M2 36/36 · M3 17/17（78 全绿）→ 修复后 92/92

## 发现并修复（14 项）

| # | 级别 | 位置 | 缺陷 | 修复 | 测试 |
|---|------|------|------|------|------|
| 32 | 🔴 Critical | M2 normalize | **Unicode 零宽/软连字符/空格族绕过执行动词检测**（ZWNJ \u200C / ZWJ \u200D / WORD JOINER \u2060 / SOFT HYPHEN \u00AD / EN-EM-THIN-HAIR SPACE \u2000-\u200A / NNBSP \u202F / MMSP \u205F 等 14 类字符插入「重启」中间 → 漏判 query） | normalize 移除零宽连接符族 + 空格族 | S24 |
| 33 | 🔴 Critical | M2 recognize | **疑问句带标点绕过**：「重启吗？」/「重启吗?」/「重启了吗？」/「重启吗！！！」判 exec（疑问词锚定末尾但标点未剥离） | isInterrogative 先剥尾部标点再判定（含 要不要/是不是/能不能/可不可以/可否） | S25 |
| 34 | 🔴 Critical | M3 ApprovalFlow | **非高危能力聚合升级崩溃**：query_status 达阈值 → new Approval({highRiskType: query_status}) 抛 HIGH_RISK_CAPABILITIES 未捕获异常 → 服务崩溃（应转审批而非崩） | 升级审批 highRiskType 归一化 'escalated'；原能力入事件载荷 | S18 |
| 35 | 🔴 Critical | M3 审批链 | **批准后 Grant 从未签发**：addVote+resolve 后无 Grant 签发代码（INV-G2「签发与执行启动同事务」领域缺口，执行方永远拿不到许可） | resolveApproval 批准 → 立即签发 Grant（绑定作业/目标/命令/哈希/有效期）+ 持久化 | S19 |
| 36 | 🟠 Major | M1 evaluateHealth | **健康状态粘滞**：cpu 恢复 0.1 后仍 degraded（只置 degraded/down 从不回 healthy） | 每次评估从干净态重算：新鲜观测正常 → healthy，无新鲜观测 → unknown | S15 |
| 37 | 🟠 Major | M1 MetricSample | **Infinity/-Infinity 被接受**（只查 NaN）→ 快照/健康判定 Infinity 污染 | Number.isFinite 校验 | S16 |
| 38 | 🟠 Major | M2 R10 | **术语翻译可整体绕过**：不注入 terminologyService 时 exec 直接放行（适配器忘注入即破 R10 链） | exec 意图（动词命中或模型 exec）无术语服务 → 抛错拒绝 | S26 |
| 39 | 🟠 Major | M2 Session | **压缩后 turns 不重置**：达 50 上限后 compress 成功但立即 recordTurn 仍抛错，会话实际无法继续 | compress 重置 turns=0 | S27 |
| 40 | 🟠 Major | M3 事件总线 | **事件总线从未接线**：handleExecIntent 注入 eventBus 后 0 事件发布（ApprovalRequested/GrantIssued/AggregationEscalated 全缺） | 全路径接线：高危→ApprovalRequested(+AggregationEscalated)、自动→GrantIssued、批准→ApprovalApproved+GrantIssued、吊销→GrantRevoked；事件补 schemaVersion+eventId+深冻结快照 | S20/S21 |
| 41 | 🟠 Major | M3 AggregationWindow | **非滑动窗口**：整体重置导致窗口内未过期事件被误清（clean@20min 在 t+35 被清），且最早事件延迟出窗 | 真滑动：prune 按事件时间戳剔除出窗事件，createdAt 滚动为最早事件 | S22 |
| 42 | 🟡 Minor | M1 LogEntry | 单条日志无长度上限（10MB 接受）→ 内存/审计放大 | MAX_LOG_MESSAGE_LENGTH 64KB，超限抛 LOG_MESSAGE_TOO_LONG | S17 |
| 43 | 🟡 Minor | M3 AggregationWindow | record() 无事件条数上限（20 万条实测通过）→ 窗口无界 DoS | AGG_WINDOW_MAX_EVENTS 10000，超限抛 AGG_WINDOW_LIMIT | S23 |
| 44 | 🟡 Minor | M3 Approval | deadline 边界不算过期（`>` 严格大于）→ 边界竞态倾向宽松 | 闭区间 `>=`，deadline 边界时刻视为过期 | S24 |
| 45 | 🟡 Minor | M3 事件目录 | 缺 ApprovalApproved/Rejected/TimedOut/GrantExpired 细分事件类（M0-D §3 目录要求） | 补齐细分事件类（GrantExpired 待 M5 定时扫描触发，类已定义） | S20 |

## 确认为正确行为（排除项，实证）

- M1 public 快照**不**泄露日志明细（includeLogs 仅非 public，S3 回归通过）
- M1 快照结构 {count, samples} 与 交接文档声明一致（消费方适配器契约不变）
- M2 否定句/疑问句锚定常见形式均正确拦截（S17/S20/S6 回归）
- M3 高危类型白名单校验正常、Grant 绑定/吊销语义正确（A3/E4 回归）

## 终态

- M1 28/28 · M2 40/40 · M3 24/24 = **92/92 全绿**
- 防御矩阵新增 4 行（Unicode 零宽/空格族、疑问标点、R10 缺失拒绝、窗口容量）
- 事件协议对齐 M0-D §3（schemaVersion + eventId + 深冻结快照 + 细分类型）
- 累计修复 45 项（7 波）

## 结论

第 7 波收敛。M1/M2/M3 达到完美收官质量线；M4 执行闭环可在本基线上开工。
