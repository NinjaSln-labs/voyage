# 中期检查报告 · 09-03

> 生成于 2026-09-01 · 数据覆盖 2026-08-26 ~ 2026-08-31（7 天整）
> 来源：oracle-arm-1 影子运行数据 · `metrics-history.jsonl` 日聚合 · `sim.log` 每轮明细 · `audit.jsonl` 能力分布

---

## 1. 意图量趋势

| 日期 | 请求量 | 意图量 | 查询占比 | 审批占比 | 日环比 |
|------|--------|--------|---------|---------|-------|
| 08-26 | 202 | 149 | 43.6% | 52.3% | — |
| 08-27 | **801** | 516 | 39.5% | 55.2% | +296% |
| 08-28 | 638 | 418 | 40.7% | 52.6% | -20.4% |
| 08-29 | 659 | 429 | 42.9% | 53.6% | +3.3% |
| 08-30 | 680 | 432 | 39.8% | 57.4% | +3.2% |
| 08-31 | 737 | 324 | 35.5% | 56.5% | +8.4% |
| **稳定期** | **~680** | **~430** | **~40%** | **~55%** | **<10% 波动** |

> 08-26 为启动日（半日数据），08-27 为满负荷首日（801 峰值）；**08-28 起进入稳态**，日均请求 ~680 条，波动 <10%。意图量 08-31 略低（324）可能因周末。

**结论**：✅ 稳定，无需干预。

---

## 2. 降级率趋势

| 日期 | 降级意图 | 降级率 |
|------|---------|-------|
| 08-26 | 8 | 5.4% |
| 08-27 | 34 | 6.6% |
| 08-28 | 35 | 8.4% |
| 08-29 | 28 | 6.5% |
| 08-30 | 38 | 8.8% |
| 08-31 | 20 | 6.2% |
| **均值** | **27.2** | **7.0%** |

降级率在 **6%~9% 之间波动**，均值 7.0%。无趋势性恶化。

**根因分析**（来自 `sim.log` `failures` 字段）：
- `commandcode`：always `empty_content`（推理模型空响应，非偶发，而是持续存在——每次约 10-30% 请求返回空 content）
- `teamorouter`：always `timeout`（偶发瞬态波动，约 10-20% 请求）
- 两者都是**推理模型 + 免费档网关**的固有特征，非架构缺陷

**建议目标值**：**降级率 ≤ 8%**（当前均值 7.0% 达标，8.8% 峰值略超可接受）

---

## 3. 时延趋势

| 日期 | p50 | p95 | p99 | max |
|------|-----|-----|-----|-----|
| 08-26 | 3697ms | 27187ms | 59383ms | 93192ms |
| 08-27 | 3179ms | 24342ms | 48122ms | 94897ms |
| 08-28 | 2988ms | 27390ms | 42912ms | 63718ms |
| 08-29 | 2769ms | 17090ms | 32918ms | 63966ms |
| 08-30 | 2439ms | 13221ms | 35285ms | 63910ms |
| 08-31 | 2409ms | 10905ms | 33044ms | 64491ms |

**趋势**：p50 从 3.7s 持续下降到 **2.4s**（收敛）。p95 从 27s 下降到 **11s**（显著改善但仍偏高）。p99 和 max 仍较高（~33s/~64s），受供应商兜底链末端 Agens free 档延迟拖累。

**建议目标值**：
- **p50 ≤ 3s**（当前 2.4s ✅）
- **p95 ≤ 15s**（当前 10.9s ✅）
- **p99 ≤ 45s**（当前 33s ✅）

---

## 4. 执行成功率

| 指标 | 08-30 | 08-31 | 趋势 |
|------|-------|-------|------|
| 执行完成 | 303 | 413 | ↑ |
| 执行失败 | 42 | 61 | ↑（比例稳定） |
| paramsIncomplete | 244 | 310 | 75~80% 的失败源于参数缺失 |
| targetUnresolved | 18 | 18 | 稳定 |
| 原始成功率 | 49.9% | 51.5% | 被 missing_param 污染 |
| **有效成功率** | **87.8%** | **87.1%** | **稳定** |

**有效成功率 ≈ 87%**，剔除 `paramsIncomplete`（参数缺失——模拟器生成的人格意图天然含不完整参数）和 `targetUnresolved`（keyVault 合成目标错配已修）后，真执行失败占比约 13%。

**建议目标值**：**有效成功率 ≥ 85%**（当前 87% ✅）

---

## 5. 聚合阈值校准建议

代码中当前目标值：

| 阈值 | 当前值 | 性质 | 建议 |
|------|--------|------|------|
| 审批时限 | 30 分钟 | 目标值 | **保留 30 分钟**（无实测依据推翻） |
| 同类 ≥3 次升级 | 3 | 目标值 | **保留 3 次**（安全策略，不靠数据校准） |
| 同类 ≥10 台资产 | 10 | 目标值 | **保留 10 台**（安全策略，不靠数据校准） |
| 聚合窗口时长 | 30 分钟 | 目标值 | **保留 30 分钟**（无实测依据推翻） |

**说明**：以上阈值属于安全策略参数，不是性能指标——不应仅因实测数据不同就调整。需要**安全评审**而非数据校准。当前数据积累期仅验证了这些阈值**不会导致系统异常**（死锁、溢出、崩溃），已验证通过。

---

## 6. 检查点逐项评估

| 检查点 | 状态 | 证据 |
|--------|------|------|
| metrics-history 日粒度稳定 | ✅ | 08-28 起日请求量 ~680，波动 <10% |
| missing_param 占比收敛 | ✅ | 08-30 存在 80.5%，08-31 下降至 75.1% |
| dupSkipped 未增长 | ✅ | 最近 10 轮 sim.log 均为 0，deadlock 恒为 0 |
| egress 审批记录首次出现 | **✅ 已确认** | 审计日志中 `egress_send` 能力 38 条 |
| 降级率不恶化 | ✅ | 稳定在 6~9%，均值 7.0% |
| 认证失败/服务端错误 | ✅ | 均为 0 |
| 时延不恶化 | ✅ | p50 从 3.7s 持续下降到 2.4s（收敛） |

**整体评估**：**通过**。系统运行稳定，指标健康，无异常趋势。

---

## 7. 提前校准建议

鉴于当前数据显示指标已稳定（满 1 周数据），建议**提前校准**而非等到 09-10：

| 指标 | 建议基线值 | 来源 |
|------|-----------|------|
| 降级率 | ≤8% | 7 日均值 7.0%，低波动 |
| p50 时延 | ≤3s | 当前 2.4s，连续下降 |
| p95 时延 | ≤15s | 当前 10.9s，持续改善 |
| 有效成功率 | ≥85% | 当前 87%，稳定 |
| 认证失败 | =0 | 全周期 0 |
| 服务端错误 | =0 | 全周期 0 |
| dupSkipped 持续 >0 告警 | 1 轮即告警 | 已实现 |

**不提前校准的阈值**：聚合窗口参数（30 分钟/3 次/10 台）— 安全策略，需安全评审而非数据校准。

---

## 8. 待关注事项

1. **opencode 恢复**（~09-14）：恢复后可增加一条供应商链，降低降级率
2. **p99/max 时延偏高**（~33s/~64s）：Agens free 档兜底延迟，属于消防通道成本，可接受
3. **commandcode 空响应持续存在**：推理模型固有特征，不影响可用性（降级链兜底正常）
4. **09-06 红队周更第二周**：观察去重池修复后第二轮生成
5. **备案下来后**：Caddy TLS 公网暴露前，用当前基线做对比（判断公网流量是否引入异常）