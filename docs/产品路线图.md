# 产品路线图（Theme-based）

> 版本：v2 · 2026-09-01 · 依据：`需求说明书-终版.md` §5
> 用途：对外一句话视角的路线图（详情与里程碑以 `需求说明书-终版.md` §5 和 `docs/版本计划.md` 为准）。

## ✅ 已完成（v0.9.0-alpha 功能收口）

- 口语化意图识别（actionClass+capability 三元分类）
- 系统观测（指标/日志/健康报告）
- 信任模型：四层准入 + 能力×角色矩阵服务端强制 + 高危双人审批
- 受限执行（重启/清理/扩容/配置变更/环境切换）+ SSH 被管机执行
- 全量审计五元组 + 哈希链 + 持久化
- 数据外传审批（egress_send/download/mail 独立安全维度）
- **确定性规则层**：关键词匹配覆写安全字段，不依赖模型概率输出
- 多模型供应商故障转移链
- 认证适配器（mTLS + WebAuthn + JWT RS256/JWKS）
- mTLS 自签 CA 本地通链 E2E 验证
- AI 红蓝对抗十轮收敛
- 全量 931 测试 / 零失败

## ⏳ 数据积累期（当前）

2026-08-27 起影子模式自动运行。目标：积累真实分布以校准聚合阈值。

| 域 | 状态 |
|----|------|
| 模拟器流量 | 每 2h 自动生成（4 人格角色 × LLM） |
| egress 审批 | 模拟器覆盖 + 确定性规则层送审 |
| 红队周更 | 周日 20:00 自动生成→去重→对抗召回验证 |
| 指标采集 | 日聚合 + 周报（降级率/时延/执行力） |
| 全量测试基线 | 931 pass / 0 fail |

## 🔜 下一步（v1.0.0 里程碑）

| 阶段 | 内容 | 预计时机 |
|------|------|---------|
| 数据积累中期检查 | 指标分布稳定、egress 审批首条记录确认 | 09-03 |
| 阈值校准 | 用实测分布替换聚合阈值目标值 | ~09-10 |
| 认证生产化 | mTLS 真实 CA 证书链接入 + @simplewebauthn 浏览器端联调（依赖外部资源） | 备案后 |
| 三集制内测 | 公开 220 + 隐藏 >50（独立双人岗）+ 红队周更集；评测门禁 100% | 阈值校准后 |
| 公网暴露 | DNS + Caddy TLS → 跑隐藏集回归 | 备案完成后 |
| 梯度放量 | 1% → 10% → 50% → 100%（成功率/时延/成本基线对比） | v1.0.0-rc |

## 🔮 未来（1→N）

- 多模型智能路由（按场景/成本路由）
- C2 任务拆解（Task 骨架已就位）
- C4 通俗/精确化结果解释
- C5/C6 知识 RAG 与 FAQ 自进化
- C15 通知推送（端口桩已注入）

---

- 北极星：非运维用户每月自主完成的查询/受限操作数（`产品说明书-终版` §7.1）
- 反指标红线：AI 误执行事故 = 0 · 审批绕过 = 0 · 高危操作未走审批 = 0
- 能力级不做清单见 `产品说明书-终版` §3.3（K8s/APM/多云/CI-CD 等）
